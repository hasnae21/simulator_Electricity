<?php 
include("functions.php");

class  Tranche{
        if($consommation>=0 && $consommation<=100)
        private $Tranche1 
        if($consommation>=101 && $consommation<=150)
        private $Tranche2
        if($consommation>=151 && $consommation<=210)
        private $Tranche3
        if($consommation>=211 && $consommation<=310)
        private $Tranche4
        if($consommation>=311 && $consommation<=510)
        private $Tranche5
        if($consommation>510)
        private $Tranche6
}
?>